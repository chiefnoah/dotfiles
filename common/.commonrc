#!/usr/bin/env sh

. "${HOME}/.commonexports"
. "${HOME}/.commonaliases"
. "${HOME}/.commonfunc"
. "${HOME}/.specificrc"  # https://github.com/JoshuaRLi/dotfiles

if xhost > /dev/null 2>&1; then
    . "${HOME}/.xsrc"
fi
