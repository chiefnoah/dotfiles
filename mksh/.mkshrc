#!/usr/bin/env mksh

. "${HOME}/.shlib"
. "${HOME}/.commonrc"

export SHELL='mksh'

# prompt
shlib_export_PS1

# history
alias hist='fc -l'
export HISTFILE="${HOME}/.mksh_history"
export HISTSIZE=100000
