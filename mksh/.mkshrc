#!/usr/bin/env mksh

. "${HOME}/.shlib"
. "${HOME}/.commonrc"

# prompt
HN="$(hostname)"
PS1='${HN} #'
(( USER_ID )) && \
    PS1='${USER}@${HN} $(shlib_posix_cur_path)$(shlib_mksh_git_branch) $ '

# history
alias hist='fc -l'
export HISTFILE="${HOME}/.mksh_history"
export HISTSIZE=100000
